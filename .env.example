# ================================================
# 🔥 VARIABLES DE ENTORNO - UFC Picks Backend
# ================================================

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=ufc_picks

# JWT Authentication
JWT_SECRET_KEY=tu_super_secreto_key_cambiar_en_produccion_12345
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# App Config
APP_ENV=development
DEBUG=True

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Redis (opcional, para cache)
# REDIS_URL=redis://localhost:6379

# ================================================
# 📝 NOTAS:
# ================================================
# 1. Reemplazá <username> y <password> con tus credenciales de MongoDB Atlas
# 2. JWT_SECRET_KEY debe ser un string random largo en producción
# 3. CORS_ORIGINS debe incluir tu frontend URL
# 4. Nunca commitear este archivo con credenciales reales
